{
  "name": "chain-equity",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun run --filter @chain-equity/frontend dev",
    "build": "bun run --filter ./contracts compile && bun run --filter @chain-equity/backend build && bun run --filter @chain-equity/frontend build",
    "test": "bun run --filter ./contracts test"
  }
}
